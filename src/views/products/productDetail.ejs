<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Productos</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
    <link href="/css/styles.css" rel="stylesheet">
    <link href="/css/productDetail.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/d9380e76ff.js" crossorigin="anonymous"></script>

    <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300&display=swap" rel="stylesheet">

</head>
<%- include("../partials/header") %>
<body>
    <main>
        <div class="main-productDetail">
            <section class="product-price">
                <img class="product-img" src="/img/products/<%= product.image %>" >
                <div class="title-prices">
                    <h1><%= product.name %></h1>
                    <div class="prices-pdetail">
                        <ul>
                            <li class="prices-description">Precio por unidad $<%= product.price %><i class="fas fa-wine-glass-alt"></i></li>
                            <% if (product.category == "cervezas") { %>
                            <li class="prices-description">Precio por pack $<%= product.price * 6 %><i class="fas fa-glass-cheers"></i></li>
                            <% }else { %>
                            <li class="prices-description">Precio al por mayor (más de 12 unidades) $150,00 c/u</li>
                            <% } %>
                        </ul>
                    </div>
                </div>
            </section>
            <section class="information">
                <div class="data-products">
                    <h2>Sobre <%= product.name %></h2>
                    <p><%= product.description %></p>
                </div>
                <div class="data">
                    <p class="data-row"><%= product.datos %></p>
                </div>
            </section>
            <div class="valoration">Valoración de los clientes</div>
            <div>4.5- "Muy buena. Buenos precios y muy rapido"</div>
            <div>5- "Excelente, super recomendado"</div>
            <div class="buttons">
            <div class="button-edit">
            <a href="/products/editar/<%= product.id %>">EDITAR</a>
            <form action="/products/delete/<%= product.id %>?_method=DELETE" method="POST">
                <button type="submit" class="button-delete">ELIMINAR</button>
            </form>
        </div>
        </div>
               <!--  <form action="/products/productCart">
                    <button type="submit" value="Agregar Al Carrito"></button>
            
            </form> -->

        
    </main>
</body>
<%- include("../partials/footer.ejs") %>

</html>